set -e -x

cp -a * ${BOSH_INSTALL_TARGET}

cd ${BOSH_INSTALL_TARGET}/bit_service

bundle_cmd=/var/vcap/packages/ruby-2.2.4/bin/bundle
$bundle_cmd install --local --deployment --without development test
